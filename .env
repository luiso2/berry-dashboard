# Berry Dashboard Configuration
# Leave empty to use local API endpoints (/api/guests)
# For production, deploy the /server folder separately and set this URL
# VITE_API_URL=https://your-api.railway.app/api
